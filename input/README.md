Place a binary PPM (P6) image named sample.ppm in this directory, or pass another PPM path with --input.

A tiny sample can be generated on Linux with ImageMagick:
convert input/sample.png -depth 8 input/sample.ppm
